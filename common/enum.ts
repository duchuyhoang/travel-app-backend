export enum PERMISSION {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum AUTH_METHOD {
  FB = "FB",
  GOOGLE = "GOOGLE",
  PASSWORD = "PASSWORD",
}

export enum DEL_FLAG {
  EXIST = 1,
  DELETED = 0,
}
